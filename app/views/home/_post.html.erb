<div class="p-4 my-4 bg-project-white border-solid shadow-md rounded-md w-[770px] flex" id="<%= dom_id post %>">

  <div class="flex flex-col">
    <div class="flex items-center">
      <img src="https://picsum.photos/40/40/?random=10" class="rounded-full">
      <div class="mx-4"><%= link_to "@#{post.user.email}", root_path, class: "text-lg" %></div>
      <div class="mx-4"><%= post.created_at %></div>
    </div>
    <h2 ><%= post.title %></h2>
  </div>
  <!--
  <p class="my-5">
    <strong class="block mb-1 font-medium">標題:</strong>
    <%= post.title %>
  </p>

  <p class="my-5">
    <strong class="block mb-1 font-medium">發布時間:</strong>
    <%= post.created_at %>
  </p>

  <p class="my-5">
    <strong class="block mb-1 font-medium">內容:</strong>
    <%= simple_format post.content %>
  </p>

  <p class="my-5">
    <strong class="block mb-1 font-medium">用戶:</strong>
    <%= post.user.email %>
  </p>

  <p class="my-5">
    <strong class="block mb-1 font-medium">標籤:</strong>
    <% post.tags.each do |tag| %>
      <%= link_to "# #{tag.name}", tag_path(id: tag.id), class:"mx-2" %>
    <% end %>
  </p>

</div>
